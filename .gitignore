# Local Arduino sketch — keep on your machine; OTA uses exported .bin only (see fw/ravens_bike/latest.json)
ravens_bike/*.ino

# Arduino / ESP32 build artifacts
build/
*.elf
*.bin
# Allow shipping OTA binaries from fw/ (still optional to commit)
!fw/ravens_bikeV*.bin

# Bundled Arduino CLI (download separately; see scripts/build-firmware.ps1)
tools/arduino-cli/
*.map
*.o
*.a

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Optional: uncomment if you use PlatformIO
# .pio/
